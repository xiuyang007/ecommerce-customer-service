# Development notes - Chapter 04

## 2026-09-16：第四章设计定稿

- 用户关键原话：`设计第四章...把客服系统的检索质量做上一个台阶:混合检索加重排,再配一套评估体系`。
- 关键产出：完成 Milvus 2.5 混合检索、BM25 中文全文检索、RRF 融合、bge-reranker-v2-m3 精排、Query 理解、元数据过滤、引用映射、低置信度拒答、低置信度问题池、四策略评估和前端引用/反馈交互的对话设计；按 `project-pipeline` 与 `project-design` 工作，不启用参考模板提到的 Superpowers。
- 用户拒绝或纠偏：本章不做指代消解和多轮改写；同义词只用于检索侧，不改变入库 chunk；技术选型固定为 Milvus 原生 BM25 + hybrid_search RRF 和 bge-reranker-v2-m3。
- 翻车与返工：本轮只做设计，没有写实现代码或伪造测试结果。Context7 CLI 当前因沙箱网络权限无法访问远端文档，因此 Milvus 的具体版本参数、BM25 Function 配置和 hybrid_search 签名在实现前必须重新通过 Context7 核验；未确认前不进入编码。

### 本章当前状态

- 设计：完成；技术边界见上文。
- 实现：已完成混合检索、重排、拒答/低置信度入池、引用和页面反馈，并包含所依赖的第三章有界工具循环。
- 验证：2026-09-23 的上传快照通过 `33 passed`、编译、浏览器脚本语法和 Compose 配置检查；本次 Docker 引擎不可达，未重新运行真实 Milvus/MySQL/上游端到端验证。历史真实链路记录不等于本次验证结果。
