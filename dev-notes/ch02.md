# Development notes - Chapter 02

## 2026-09-15：第二章设计定稿

- 用户关键原话：`开始设计项目第二章...用 Function Calling 让模型自己决定调什么工具`。
- 关键产出：明确 FastAPI + SQLAlchemy + MySQL(Docker) + LangChain @tool 的分层、四张表、五个工具、SSE 工具状态事件、单轮调用边界和三条验收路径；设计只在对话中交付，没有启用参考模板提到的 Superpowers skill。
- 用户拒绝或纠偏：本章不做 Agent Loop、多轮工具循环、RAG 或向量检索；技术选型不允许自行替换。
- 翻车与返工：环境检查发现 Docker/WSL2 未安装，本机 MySQL80 虽在运行，但没有擅自把 Docker 技术栈换成本机 MySQL。

## 2026-09-15：数据库运行环境准备

- 用户关键原话：`运行`。
- 关键产出：通过 `winget` 安装 Docker Desktop 4.91.0；安装 WSL 组件；以管理员权限开启 `VirtualMachinePlatform` 和 `Microsoft-Windows-Subsystem-Linux` Windows 功能。
- 关键验证：Docker CLI `29.8.0`、Docker Compose `v5.5.1` 已安装；Windows 功能启用命令返回 `3010`，表示必须重启系统后 WSL2 才能生效。
- 用户拒绝或纠偏：没有在未重启的情况下改用本机 MySQL 冒充 Docker MySQL；没有绕过 Windows 功能或 UAC。
- 翻车与返工：当前无法启动 MySQL 容器，需重启 Windows 后执行 `scripts/start_ch02.ps1`。

## 2026-09-15：第二章核心实现批次

- 用户关键原话：`下一步`。
- 关键产出：新增 MySQL Compose 与初始化 SQL、SQLAlchemy ORM 四表、异步仓储、五个 `@tool`、工具注册表、参数 Schema 校验、超时重试、单轮 Function Calling 编排、messages 工具轨迹持久化，以及聊天页工具徽章事件。
- 关键验证：`python -m compileall -q app tests scripts` 通过；`python -m pytest -q` 为 `26 passed`；浏览器 JavaScript 语法检查通过。
- 用户拒绝或纠偏：最终回答仍由工具结果回灌后的模型流式生成；工具最多执行一次；`create_ticket` 不自动重试。
- 翻车与返工：真实 GLM 工具链测试返回 `429`，上游明确为“余额不足或无可用资源包”，因此三条浏览器验收的真实模型答案暂未通过；没有切换模型或伪造通过结果。

## 2026-09-15：真实 MySQL 与 DeepSeek 验收完成

- 用户关键原话：`允许重启`、`继续`、`换一个api-key`、`base_url...`、`允许`。
- 关键产出：Docker Desktop、WSL2 和 MySQL 8.4 容器启动成功；四张表与 5 条 FAQ 种子数据落库；当前模型切换为 DeepSeek OpenAI 兼容配置 `https://api.deepseek.com` + `deepseek-flash`；结构化输出切换为实测可用的 `json_mode`。
- 关键验证：`scripts/verify_all.ps1` 最终输出 `ALL_CHECKS_PASSED`；真实 DeepSeek 工具链三条验收全部通过；MySQL 中确认 conversations、messages、tickets 均有真实数据，工具消息和工单成功落库。
- 用户拒绝或纠偏：`/anthropic` 地址与当前 OpenAI 协议技术栈冲突，没有切换到 Anthropic SDK；改用 DeepSeek 官方 OpenAI 兼容地址。
- 翻车与返工：首次 FAQ 查询漏命中，根因是 MySQL 初始化脚本缺少 `SET NAMES utf8mb4`，中文种子数据被错误编码写入；已修正初始化脚本并重灌验证。DeepSeek 思考模式不支持 `tool_choice`，因此结构化提取从 `function_calling` 切换为真实可用的 `json_mode`。

### 本次测试示例

1. `订单 1001 的物流到哪了` -> 模型调用 `query_logistics`，页面显示工具徽章和顺丰物流结果。
2. `退货政策是什么` -> 模型调用 `query_faq`，MySQL 命中“签收后 7 天内可申请退货”。
3. `邮费是多少` -> 模型调用 `query_faq`，工具返回空列表，页面明确回答未查到；这是预期漏召回。
4. MySQL 四个工具直接执行 -> `query_order`、`query_product`、`query_logistics`、`query_faq` 均返回成功。
5. `create_ticket` -> 成功创建 `T202609151487A617`，状态为 `pending`。
6. `python -m pytest -q` -> `26 passed`。
7. `scripts/verify_all.ps1` -> `ALL_CHECKS_PASSED`。

### 早期测试示例

1. `订单 1001 的物流到哪了`：工具执行器单元测试验证 `query_logistics` 返回订单号 `1001`。
2. `退货政策是什么`：SQLite 隔离仓储测试验证 `query_faq` 命中 FAQ。
3. `邮费是多少`：SQLite 隔离仓储测试验证 LIKE 查询返回空列表，作为预期漏召回。
4. `create_ticket`：验证工单写入且状态为 `pending`。
5. 未知工具、参数校验、超时、可重试工具：工具执行器测试覆盖。
6. 单轮编排测试：确认事件顺序为 `meta -> tool_status -> tool_result -> delta -> done`。

## 2026-09-15：页面 `Failed to fetch` 诊断

- 用户关键原话：`分析`。
- 关键产出：确认客户端的 `Failed to fetch` 是后端 Uvicorn 已停止导致，不是 DeepSeek、工具链或 MySQL 故障；MySQL 容器保持 `healthy`。
- 关键验证：诊断时 `127.0.0.1:8767` 没有监听进程，访问 `/health` 返回连接被拒绝；重新启动 Uvicorn 后，在内置浏览器发送 `你好`，页面成功显示 DeepSeek 回复和“已完成”。
- 用户拒绝或纠偏：没有把浏览器中的旧 `localStorage` 展示误判为服务端当前会话；数据库历史仍以 MySQL 的 conversations/messages 为准。
- 翻车与返工：没有代码返工；当前运行服务需要用户终止或保持运行，关闭终端后浏览器再次请求会出现同类连接错误。

## 2026-09-15：数据库表只读面板

- 用户关键原话：`把你剪的四个数据库表，显示在当前页面`。
- 关键产出：新增 `GET /api/v1/database/tables?limit=20` 只读接口；聊天页新增“数据表”页签，展示 `faq`、`conversations`、`messages`、`tickets` 的列名、总量和最近 20 条记录。
- 关键验证：`python -m pytest -q` 为 `28 passed`；浏览器 JavaScript 语法检查通过。接口在 MySQL 可用时返回四张表，在 Docker 守护进程停止时按设计返回 `503 database unavailable`。
- 用户拒绝或纠偏：没有提供编辑或删除按钮；数据库面板只做只读展示。
- 翻车与返工：会话切换后 Uvicorn 和 Docker Desktop 均被回收。重启 Uvicorn 后接口正常加载，但 Docker 守护进程未启动，`com.docker.service` 普通权限启动被拒绝，管理员提升进程报 `0xc0000142`；因此最终可视化验收待 Docker 服务恢复。

## 2026-09-15：工具徽章挤压气泡修复

- 用户关键原话：`这种回答的格式不对`。
- 关键产出：确认不是模型输出格式错误，而是 assistant 气泡与工具轨迹作为同一 flex 行的兄弟元素时，工具轨迹的固定 flex-basis 把气泡挤压成竖条；已为 `.message-row` 开启换行，并把 `.tool-trace` 固定到气泡下方独立一行。
- 关键验证：刷新页面后旧文字记录宽度恢复正常；重新发送“订单 1001 的物流到哪了”，assistant 气泡保持正常宽度，`query_logistics` 徽章显示在气泡下方，最终回答正常流式输出。
- 用户拒绝或纠偏：没有修改模型 Prompt 去掩盖 CSS 布局问题，也没有删除 localStorage 里的测试记录。
- 翻车与返工：历史聊天记录只保存文字，不保存工具徽章，因此刷新后旧轮次不会恢复徽章；本轮只修复新产生的徽章布局，是否持久化徽章留待后续需要时处理。
